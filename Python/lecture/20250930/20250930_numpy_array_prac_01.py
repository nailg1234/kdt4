# 배열 연산
# 1. 다음 배열을 생성하고, 모든 요소에 3을 더하세요

import numpy as np

arr = np.array([1, 2, 3, 4])
arr += 3
print('1. 다음 배열을 생성하고, 모든 요소에 3을 더하세요\n', arr)

# 2. 아래 2차원 각 요소를 -1로 곱한 새로운 배열을 만드세요.
arr = np.array([
    [5, 10],
    [15, 20]
])
