# Day 2-1교시: switch 문

## 학습 목표
- switch 문의 구조와 동작 원리 이해하기
- break 문의 역할과 fall-through 이해하기
- switch vs if-else 비교하고 상황에 맞게 선택하기
- 실전 프로그램에 switch 문 활용하기

---

## 핵심 개념 미리보기

```
┌────────────────────────────────────────────────────────────────────────┐
│                       switch 문 구조                                    │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  switch (변수) {                                                       │
│      case 값1:   ───────────→  값1과 일치하면 실행                      │
│          명령문;                                                       │
│          break;  ───────────→  switch 종료                             │
│      case 값2:   ───────────→  값2와 일치하면 실행                      │
│          명령문;                                                       │
│          break;                                                        │
│      default:    ───────────→  모두 불일치시 실행                       │
│          명령문;                                                       │
│  }                                                                     │
│                                                                        │
├────────────────────────────────────────────────────────────────────────┤
│  switch 사용 가능 타입:    int, char, short, long, enum                │
│  switch 사용 불가 타입:    float, double, string, 배열                  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 1. switch 문 기본

### 1.1 switch 문 흐름도

```
        ┌─────────────┐
        │    시작     │
        └──────┬──────┘
               │
               ▼
        ┌─────────────┐
        │  변수 평가   │
        └──────┬──────┘
               │
    ┌──────────┼──────────┬──────────┐
    │          │          │          │
 값1?▼       값2?▼      값3?▼      기타▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ case 1 │ │ case 2 │ │ case 3 │ │default │
│  실행  │ │  실행  │ │  실행  │ │  실행  │
└────┬───┘ └────┬───┘ └────┬───┘ └────┬───┘
     │          │          │          │
     │  break   │  break   │  break   │
     └──────────┴──────────┴──────────┘
                    │
                    ▼
            ┌─────────────┐
            │    종료     │
            └─────────────┘
```

### 1.2 기본 문법

```cpp
switch (변수 또는 식) {
    case 값1:
        // 값1일 때 실행할 코드
        break;
    case 값2:
        // 값2일 때 실행할 코드
        break;
    case 값3:
        // 값3일 때 실행할 코드
        break;
    default:
        // 어떤 case에도 해당하지 않을 때
        break;
}
```

### 1.3 switch 문의 특징

```
┌────────────────────────────────────────────────────────────────────────┐
│                       switch 문의 특징                                  │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  1. 정확한 값 매칭                                                      │
│     ┌─────────────────────────────────────────────────────────────┐   │
│     │ switch(x)는 x == 값 비교만 가능                              │   │
│     │ x > 10, x >= 5 같은 범위 비교는 불가                         │   │
│     └─────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  2. 정수 계열 타입만 가능                                               │
│     ┌─────────────────────────────────────────────────────────────┐   │
│     │ ✓ int, char, short, long, enum                              │   │
│     │ ✗ float, double, string                                     │   │
│     └─────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  3. case 값은 상수만 가능                                               │
│     ┌─────────────────────────────────────────────────────────────┐   │
│     │ ✓ case 1:, case 'A':, case CONSTANT:                        │   │
│     │ ✗ case x:, case getValue():                                 │   │
│     └─────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  4. 중복 case 불가                                                      │
│     ┌─────────────────────────────────────────────────────────────┐   │
│     │ case 1: ... case 1: // 컴파일 에러!                         │   │
│     └─────────────────────────────────────────────────────────────┘   │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 1.4 요일 출력 예제

```cpp
#include <iostream>
using namespace std;

int main() {
    int day;

    cout << "요일 번호를 입력하세요 (1-7): ";
    cin >> day;

    switch (day) {
        case 1:
            cout << "월요일입니다." << endl;
            break;
        case 2:
            cout << "화요일입니다." << endl;
            break;
        case 3:
            cout << "수요일입니다." << endl;
            break;
        case 4:
            cout << "목요일입니다." << endl;
            break;
        case 5:
            cout << "금요일입니다." << endl;
            break;
        case 6:
            cout << "토요일입니다." << endl;
            break;
        case 7:
            cout << "일요일입니다." << endl;
            break;
        default:
            cout << "잘못된 입력입니다. (1-7 사이 입력)" << endl;
            break;
    }

    return 0;
}
```

**실행 결과:**
```
요일 번호를 입력하세요 (1-7): 3
수요일입니다.
```

---

## 2. break 문의 중요성

### 2.1 break의 역할

```
break 있을 때:                    break 없을 때 (Fall-through):

switch (n) {  // n = 2            switch (n) {  // n = 2
    case 1:                           case 1:
        실행1;                            실행1;
        break; ←── 종료                   // break 없음
    case 2:                           case 2:
        실행2; ←── 여기만 실행            실행2; ←── 시작
        break; ←── 종료                   // break 없음
    case 3:                           case 3:
        실행3;                            실행3; ←── 계속 실행!
        break;                            // break 없음
    default:                          default:
        실행D;                            실행D; ←── 계속 실행!
}                                 }

결과: 실행2                       결과: 실행2 → 실행3 → 실행D
```

### 2.2 break 유무 비교

```cpp
#include <iostream>
using namespace std;

int main() {
    int number = 2;

    cout << "=== break 없는 경우 (Fall-through) ===" << endl;
    switch (number) {
        case 1:
            cout << "하나" << endl;
        case 2:
            cout << "둘" << endl;      // 여기서 시작
        case 3:
            cout << "셋" << endl;      // 계속 실행됨
        default:
            cout << "기타" << endl;    // 여기도 실행됨
    }

    cout << "\n=== break 있는 경우 ===" << endl;
    switch (number) {
        case 1:
            cout << "하나" << endl;
            break;
        case 2:
            cout << "둘" << endl;      // 여기만 실행
            break;
        case 3:
            cout << "셋" << endl;
            break;
        default:
            cout << "기타" << endl;
            break;
    }

    return 0;
}
```

**실행 결과:**
```
=== break 없는 경우 (Fall-through) ===
둘
셋
기타

=== break 있는 경우 ===
둘
```

### 2.3 의도적인 Fall-through 활용

여러 case가 같은 동작을 할 때 Fall-through를 활용합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int month;

    cout << "월을 입력하세요 (1-12): ";
    cin >> month;

    switch (month) {
        // 겨울: 12, 1, 2월
        case 12:
        case 1:
        case 2:
            cout << "겨울입니다. 따뜻하게 입으세요!" << endl;
            break;

        // 봄: 3, 4, 5월
        case 3:
        case 4:
        case 5:
            cout << "봄입니다. 꽃구경 가세요!" << endl;
            break;

        // 여름: 6, 7, 8월
        case 6:
        case 7:
        case 8:
            cout << "여름입니다. 시원하게 보내세요!" << endl;
            break;

        // 가을: 9, 10, 11월
        case 9:
        case 10:
        case 11:
            cout << "가을입니다. 단풍 구경 가세요!" << endl;
            break;

        default:
            cout << "잘못된 월입니다." << endl;
            break;
    }

    return 0;
}
```

**실행 결과:**
```
월을 입력하세요 (1-12): 7
여름입니다. 시원하게 보내세요!
```

### 2.4 Fall-through 시각화

```
month = 7일 때의 실행 흐름:

switch (month) {
    case 12:  ←── 7 ≠ 12, 건너뜀
    case 1:   ←── 7 ≠ 1, 건너뜀
    case 2:   ←── 7 ≠ 2, 건너뜀
        ...
    case 6:   ←── 7 ≠ 6, 건너뜀
    case 7:   ←── 7 == 7, 여기서 진입!
              │
    case 8:   ←── fall-through (case만 있고 코드 없음)
              │
        cout << "여름입니다...";
        break;  ←── 여기서 switch 종료
    ...
}

┌─────────────────────────────────────────────────────────────────┐
│  Fall-through가 유용한 경우:                                     │
│  • 여러 값이 같은 동작을 할 때                                    │
│  • 코드 중복을 줄이고 싶을 때                                     │
│                                                                 │
│  주의: 의도적인 Fall-through라면 주석으로 명시하는 것이 좋습니다.  │
│        // fall-through 또는 [[fallthrough]]; (C++17)           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 문자형 switch

### 3.1 문자(char)를 이용한 switch

```cpp
#include <iostream>
using namespace std;

int main() {
    char grade;

    cout << "학점을 입력하세요 (A-F): ";
    cin >> grade;

    switch (grade) {
        case 'A':
        case 'a':
            cout << "우수한 성적입니다! (90점 이상)" << endl;
            break;
        case 'B':
        case 'b':
            cout << "좋은 성적입니다! (80-89점)" << endl;
            break;
        case 'C':
        case 'c':
            cout << "보통 성적입니다. (70-79점)" << endl;
            break;
        case 'D':
        case 'd':
            cout << "분발이 필요합니다. (60-69점)" << endl;
            break;
        case 'F':
        case 'f':
            cout << "불합격입니다. (60점 미만)" << endl;
            break;
        default:
            cout << "잘못된 학점입니다. (A, B, C, D, F만 입력)" << endl;
            break;
    }

    return 0;
}
```

**실행 결과:**
```
학점을 입력하세요 (A-F): a
우수한 성적입니다! (90점 이상)
```

### 3.2 간단한 계산기

```cpp
#include <iostream>
using namespace std;

int main() {
    double num1, num2;
    char op;

    cout << "첫 번째 숫자: ";
    cin >> num1;

    cout << "연산자 (+, -, *, /): ";
    cin >> op;

    cout << "두 번째 숫자: ";
    cin >> num2;

    cout << "\n결과: " << num1 << " " << op << " " << num2 << " = ";

    switch (op) {
        case '+':
            cout << num1 + num2 << endl;
            break;
        case '-':
            cout << num1 - num2 << endl;
            break;
        case '*':
            cout << num1 * num2 << endl;
            break;
        case '/':
            if (num2 != 0) {
                cout << num1 / num2 << endl;
            } else {
                cout << "오류! 0으로 나눌 수 없습니다." << endl;
            }
            break;
        default:
            cout << "\n잘못된 연산자입니다!" << endl;
            break;
    }

    return 0;
}
```

**실행 결과:**
```
첫 번째 숫자: 10
연산자 (+, -, *, /): *
두 번째 숫자: 5

결과: 10 * 5 = 50
```

### 3.3 메뉴 시스템

```cpp
#include <iostream>
using namespace std;

int main() {
    char choice;

    cout << "╔═══════════════════════════════╗" << endl;
    cout << "║        메인 메뉴              ║" << endl;
    cout << "╠═══════════════════════════════╣" << endl;
    cout << "║  N - 새 파일                  ║" << endl;
    cout << "║  O - 파일 열기                ║" << endl;
    cout << "║  S - 저장                     ║" << endl;
    cout << "║  P - 인쇄                     ║" << endl;
    cout << "║  Q - 종료                     ║" << endl;
    cout << "╚═══════════════════════════════╝" << endl;
    cout << "선택: ";
    cin >> choice;

    switch (choice) {
        case 'N':
        case 'n':
            cout << "\n새 파일을 생성합니다..." << endl;
            break;
        case 'O':
        case 'o':
            cout << "\n파일 열기 대화상자를 엽니다..." << endl;
            break;
        case 'S':
        case 's':
            cout << "\n파일을 저장합니다..." << endl;
            break;
        case 'P':
        case 'p':
            cout << "\n인쇄를 시작합니다..." << endl;
            break;
        case 'Q':
        case 'q':
            cout << "\n프로그램을 종료합니다." << endl;
            break;
        default:
            cout << "\n잘못된 선택입니다. 다시 시도하세요." << endl;
            break;
    }

    return 0;
}
```

---

## 4. switch vs if-else 비교

### 4.1 비교 표

```
┌───────────────────┬──────────────────────┬──────────────────────┐
│       기준        │       switch         │       if-else        │
├───────────────────┼──────────────────────┼──────────────────────┤
│ 비교 방식         │ 정확한 값 일치       │ 조건식 (범위 가능)   │
├───────────────────┼──────────────────────┼──────────────────────┤
│ 사용 가능 타입    │ 정수, 문자, enum     │ 모든 타입            │
├───────────────────┼──────────────────────┼──────────────────────┤
│ 조건 개수가       │ 가독성 좋음          │ 복잡해짐             │
│ 많을 때           │                      │                      │
├───────────────────┼──────────────────────┼──────────────────────┤
│ 범위 비교         │ 불가능               │ 가능                 │
│ (score >= 90)     │                      │                      │
├───────────────────┼──────────────────────┼──────────────────────┤
│ 복합 조건         │ 불가능               │ 가능                 │
│ (a > 0 && b < 10) │                      │                      │
├───────────────────┼──────────────────────┼──────────────────────┤
│ 성능              │ 약간 빠름            │ 비슷함               │
│ (점프 테이블)     │                      │                      │
└───────────────────┴──────────────────────┴──────────────────────┘
```

### 4.2 switch가 적합한 경우

```cpp
// 메뉴 선택 - switch 권장
switch (choice) {
    case 1:
        cout << "게임 시작" << endl;
        break;
    case 2:
        cout << "설정" << endl;
        break;
    case 3:
        cout << "종료" << endl;
        break;
    default:
        cout << "잘못된 선택" << endl;
        break;
}

// if-else로 작성하면?
if (choice == 1) {
    cout << "게임 시작" << endl;
} else if (choice == 2) {
    cout << "설정" << endl;
} else if (choice == 3) {
    cout << "종료" << endl;
} else {
    cout << "잘못된 선택" << endl;
}
```

### 4.3 if-else가 적합한 경우

```cpp
// 점수 범위 판정 - if-else 권장
if (score >= 90) {
    cout << "A" << endl;
} else if (score >= 80) {
    cout << "B" << endl;
} else if (score >= 70) {
    cout << "C" << endl;
} else if (score >= 60) {
    cout << "D" << endl;
} else {
    cout << "F" << endl;
}

// switch로는 불가능! (범위 비교 안됨)
// switch (score >= 90) 이런 식도 불가

// 복합 조건 - if-else만 가능
if (age >= 18 && hasLicense) {
    cout << "운전 가능" << endl;
}
```

### 4.4 선택 가이드

```
┌─────────────────────────────────────────────────────────────────────┐
│                    switch vs if-else 선택 가이드                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  switch를 선택하세요:                                                │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ ✓ 하나의 변수를 여러 특정 값과 비교할 때                      │   │
│  │ ✓ 메뉴 선택, 요일, 월, 상태 코드 등                          │   │
│  │ ✓ 비교할 case가 3개 이상일 때                                │   │
│  │ ✓ 정수나 문자 타입일 때                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  if-else를 선택하세요:                                               │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ ✓ 범위 비교가 필요할 때 (>=, <=, >, <)                       │   │
│  │ ✓ 복합 조건이 필요할 때 (&&, ||)                             │   │
│  │ ✓ 실수나 문자열 비교가 필요할 때                              │   │
│  │ ✓ 조건이 2개 이하일 때                                       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 5. 실전 예제

### 5.1 자판기 프로그램

```cpp
#include <iostream>
using namespace std;

int main() {
    int choice;
    int money;

    cout << "╔════════════════════════════════╗" << endl;
    cout << "║         음료 자판기            ║" << endl;
    cout << "╠════════════════════════════════╣" << endl;
    cout << "║  1. 콜라      - 1,000원        ║" << endl;
    cout << "║  2. 사이다    - 1,100원        ║" << endl;
    cout << "║  3. 커피      - 1,500원        ║" << endl;
    cout << "║  4. 녹차      - 1,200원        ║" << endl;
    cout << "║  5. 주스      - 2,000원        ║" << endl;
    cout << "╚════════════════════════════════╝" << endl;

    cout << "\n음료를 선택하세요 (1-5): ";
    cin >> choice;

    int price;
    string itemName;

    switch (choice) {
        case 1:
            itemName = "콜라";
            price = 1000;
            break;
        case 2:
            itemName = "사이다";
            price = 1100;
            break;
        case 3:
            itemName = "커피";
            price = 1500;
            break;
        case 4:
            itemName = "녹차";
            price = 1200;
            break;
        case 5:
            itemName = "주스";
            price = 2000;
            break;
        default:
            cout << "잘못된 선택입니다." << endl;
            return 1;
    }

    cout << itemName << "를 선택하셨습니다. (" << price << "원)" << endl;
    cout << "금액을 투입하세요: ";
    cin >> money;

    if (money >= price) {
        int change = money - price;
        cout << "\n" << itemName << "가 나왔습니다!" << endl;
        if (change > 0) {
            cout << "거스름돈: " << change << "원" << endl;
        }
    } else {
        int shortage = price - money;
        cout << "\n금액이 " << shortage << "원 부족합니다." << endl;
        cout << "투입한 " << money << "원을 반환합니다." << endl;
    }

    return 0;
}
```

**실행 결과:**
```
╔════════════════════════════════╗
║         음료 자판기            ║
╠════════════════════════════════╣
║  1. 콜라      - 1,000원        ║
║  2. 사이다    - 1,100원        ║
║  3. 커피      - 1,500원        ║
║  4. 녹차      - 1,200원        ║
║  5. 주스      - 2,000원        ║
╚════════════════════════════════╝

음료를 선택하세요 (1-5): 3
커피를 선택하셨습니다. (1500원)
금액을 투입하세요: 2000

커피가 나왔습니다!
거스름돈: 500원
```

### 5.2 달의 일수 계산

```cpp
#include <iostream>
using namespace std;

int main() {
    int month, year;

    cout << "연도를 입력하세요: ";
    cin >> year;
    cout << "월을 입력하세요 (1-12): ";
    cin >> month;

    int days;
    bool valid = true;

    switch (month) {
        // 31일인 달
        case 1: case 3: case 5: case 7:
        case 8: case 10: case 12:
            days = 31;
            break;

        // 30일인 달
        case 4: case 6: case 9: case 11:
            days = 30;
            break;

        // 2월 (윤년 확인 필요)
        case 2:
            // 윤년 조건: 4로 나누어지고, 100으로 안 나누어지거나 400으로 나누어짐
            if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
                days = 29;  // 윤년
            } else {
                days = 28;  // 평년
            }
            break;

        default:
            valid = false;
            break;
    }

    if (valid) {
        cout << year << "년 " << month << "월은 " << days << "일까지 있습니다." << endl;

        if (month == 2) {
            if (days == 29) {
                cout << "(" << year << "년은 윤년입니다)" << endl;
            } else {
                cout << "(" << year << "년은 평년입니다)" << endl;
            }
        }
    } else {
        cout << "잘못된 월입니다. (1-12 사이 입력)" << endl;
    }

    return 0;
}
```

**실행 결과:**
```
연도를 입력하세요: 2024
월을 입력하세요 (1-12): 2
2024년 2월은 29일까지 있습니다.
(2024년은 윤년입니다)
```

### 5.3 가위바위보 게임

```cpp
#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main() {
    srand(time(0));  // 난수 시드 초기화

    int user, computer;

    cout << "╔════════════════════════════════╗" << endl;
    cout << "║       가위바위보 게임          ║" << endl;
    cout << "╠════════════════════════════════╣" << endl;
    cout << "║  1. 가위                       ║" << endl;
    cout << "║  2. 바위                       ║" << endl;
    cout << "║  3. 보                         ║" << endl;
    cout << "╚════════════════════════════════╝" << endl;

    cout << "\n선택하세요 (1-3): ";
    cin >> user;

    computer = rand() % 3 + 1;  // 1~3 랜덤

    // 사용자 선택 출력
    cout << "\n당신: ";
    switch (user) {
        case 1: cout << "가위 ✌️" << endl; break;
        case 2: cout << "바위 ✊" << endl; break;
        case 3: cout << "보 ✋" << endl; break;
        default:
            cout << "잘못된 선택!" << endl;
            return 1;
    }

    // 컴퓨터 선택 출력
    cout << "컴퓨터: ";
    switch (computer) {
        case 1: cout << "가위 ✌️" << endl; break;
        case 2: cout << "바위 ✊" << endl; break;
        case 3: cout << "보 ✋" << endl; break;
    }

    // 결과 판정
    cout << "\n결과: ";
    if (user == computer) {
        cout << "무승부!" << endl;
    } else {
        // 승리 조건: (가위-보), (바위-가위), (보-바위)
        switch (user) {
            case 1:  // 가위
                if (computer == 3) cout << "승리! 🎉" << endl;
                else cout << "패배... 😢" << endl;
                break;
            case 2:  // 바위
                if (computer == 1) cout << "승리! 🎉" << endl;
                else cout << "패배... 😢" << endl;
                break;
            case 3:  // 보
                if (computer == 2) cout << "승리! 🎉" << endl;
                else cout << "패배... 😢" << endl;
                break;
        }
    }

    return 0;
}
```

### 5.4 단위 변환기

```cpp
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int category, unit;
    double value;

    cout << "╔════════════════════════════════╗" << endl;
    cout << "║         단위 변환기            ║" << endl;
    cout << "╠════════════════════════════════╣" << endl;
    cout << "║  1. 길이 변환                  ║" << endl;
    cout << "║  2. 무게 변환                  ║" << endl;
    cout << "║  3. 온도 변환                  ║" << endl;
    cout << "╚════════════════════════════════╝" << endl;

    cout << "\n카테고리 선택: ";
    cin >> category;

    switch (category) {
        case 1:  // 길이
            cout << "\n[길이 변환]" << endl;
            cout << "1. km → miles" << endl;
            cout << "2. miles → km" << endl;
            cout << "3. m → feet" << endl;
            cout << "4. feet → m" << endl;
            cout << "선택: ";
            cin >> unit;
            cout << "값 입력: ";
            cin >> value;

            cout << fixed << setprecision(2);
            switch (unit) {
                case 1:
                    cout << value << " km = " << value * 0.621371 << " miles" << endl;
                    break;
                case 2:
                    cout << value << " miles = " << value * 1.60934 << " km" << endl;
                    break;
                case 3:
                    cout << value << " m = " << value * 3.28084 << " feet" << endl;
                    break;
                case 4:
                    cout << value << " feet = " << value * 0.3048 << " m" << endl;
                    break;
                default:
                    cout << "잘못된 선택" << endl;
            }
            break;

        case 2:  // 무게
            cout << "\n[무게 변환]" << endl;
            cout << "1. kg → lbs" << endl;
            cout << "2. lbs → kg" << endl;
            cout << "3. g → oz" << endl;
            cout << "선택: ";
            cin >> unit;
            cout << "값 입력: ";
            cin >> value;

            cout << fixed << setprecision(2);
            switch (unit) {
                case 1:
                    cout << value << " kg = " << value * 2.20462 << " lbs" << endl;
                    break;
                case 2:
                    cout << value << " lbs = " << value * 0.453592 << " kg" << endl;
                    break;
                case 3:
                    cout << value << " g = " << value * 0.035274 << " oz" << endl;
                    break;
                default:
                    cout << "잘못된 선택" << endl;
            }
            break;

        case 3:  // 온도
            cout << "\n[온도 변환]" << endl;
            cout << "1. 섭씨(°C) → 화씨(°F)" << endl;
            cout << "2. 화씨(°F) → 섭씨(°C)" << endl;
            cout << "선택: ";
            cin >> unit;
            cout << "값 입력: ";
            cin >> value;

            cout << fixed << setprecision(1);
            switch (unit) {
                case 1:
                    cout << value << "°C = " << (value * 9/5 + 32) << "°F" << endl;
                    break;
                case 2:
                    cout << value << "°F = " << ((value - 32) * 5/9) << "°C" << endl;
                    break;
                default:
                    cout << "잘못된 선택" << endl;
            }
            break;

        default:
            cout << "잘못된 카테고리입니다." << endl;
    }

    return 0;
}
```

---

## 6. 연습 문제

### 문제 1: 요일 분류
요일 번호(1-7)를 입력받아 평일/주말을 구분하세요.

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
using namespace std;

int main() {
    int day;

    cout << "요일 번호 (1:월 ~ 7:일): ";
    cin >> day;

    switch (day) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
            cout << "평일입니다. 열심히 일/공부하세요!" << endl;
            break;
        case 6:
        case 7:
            cout << "주말입니다. 푹 쉬세요!" << endl;
            break;
        default:
            cout << "잘못된 입력입니다." << endl;
            break;
    }

    return 0;
}
```
</details>

### 문제 2: 교통카드 요금
나이를 입력받아 교통카드 요금을 계산하세요.
- 1: 어린이 (450원)
- 2: 청소년 (720원)
- 3: 성인 (1250원)
- 4: 경로 (무료)

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
using namespace std;

int main() {
    int type;

    cout << "유형 선택:" << endl;
    cout << "1. 어린이 (만 6-12세)" << endl;
    cout << "2. 청소년 (만 13-18세)" << endl;
    cout << "3. 성인 (만 19세 이상)" << endl;
    cout << "4. 경로 (만 65세 이상)" << endl;
    cout << "선택: ";
    cin >> type;

    int fare;

    switch (type) {
        case 1:
            fare = 450;
            cout << "어린이 요금: " << fare << "원" << endl;
            break;
        case 2:
            fare = 720;
            cout << "청소년 요금: " << fare << "원" << endl;
            break;
        case 3:
            fare = 1250;
            cout << "성인 요금: " << fare << "원" << endl;
            break;
        case 4:
            fare = 0;
            cout << "경로 우대: 무료" << endl;
            break;
        default:
            cout << "잘못된 선택입니다." << endl;
            break;
    }

    return 0;
}
```
</details>

### 문제 3: 성적 등급 변환
숫자 등급(1-9)을 입력받아 학점으로 변환하세요.
- 1: A+, 2: A0, 3: A-
- 4: B+, 5: B0, 6: B-
- 7: C+, 8: C0, 9: F

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
using namespace std;

int main() {
    int grade;

    cout << "등급 번호 (1-9): ";
    cin >> grade;

    cout << "학점: ";

    switch (grade) {
        case 1:
            cout << "A+" << endl;
            break;
        case 2:
            cout << "A0" << endl;
            break;
        case 3:
            cout << "A-" << endl;
            break;
        case 4:
            cout << "B+" << endl;
            break;
        case 5:
            cout << "B0" << endl;
            break;
        case 6:
            cout << "B-" << endl;
            break;
        case 7:
            cout << "C+" << endl;
            break;
        case 8:
            cout << "C0" << endl;
            break;
        case 9:
            cout << "F" << endl;
            break;
        default:
            cout << "잘못된 등급" << endl;
            break;
    }

    return 0;
}
```
</details>

### 문제 4: 사칙연산 + 나머지 계산기
연산자에 % (나머지)를 추가한 계산기를 만드세요. (정수 연산)

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
using namespace std;

int main() {
    int num1, num2;
    char op;

    cout << "첫 번째 정수: ";
    cin >> num1;
    cout << "연산자 (+, -, *, /, %): ";
    cin >> op;
    cout << "두 번째 정수: ";
    cin >> num2;

    cout << num1 << " " << op << " " << num2 << " = ";

    switch (op) {
        case '+':
            cout << num1 + num2 << endl;
            break;
        case '-':
            cout << num1 - num2 << endl;
            break;
        case '*':
            cout << num1 * num2 << endl;
            break;
        case '/':
            if (num2 != 0) {
                cout << num1 / num2 << endl;
            } else {
                cout << "오류! 0으로 나눌 수 없습니다." << endl;
            }
            break;
        case '%':
            if (num2 != 0) {
                cout << num1 % num2 << endl;
            } else {
                cout << "오류! 0으로 나눌 수 없습니다." << endl;
            }
            break;
        default:
            cout << "잘못된 연산자입니다." << endl;
            break;
    }

    return 0;
}
```
</details>

---

## 7. FAQ

### Q1: switch에서 문자열(string)을 사용할 수 없나요?
**A:** C++에서는 switch에 string을 직접 사용할 수 없습니다. 대안:
```cpp
// 방법 1: if-else 사용
string cmd;
cin >> cmd;

if (cmd == "start") {
    // ...
} else if (cmd == "stop") {
    // ...
}

// 방법 2: 문자열을 정수 코드로 변환
// (해시 함수나 맵 사용)
```

### Q2: case에 변수를 사용할 수 있나요?
**A:** 아니요, case 값은 반드시 **컴파일 타임 상수**여야 합니다.
```cpp
int x = 5;
const int Y = 10;

switch (value) {
    case x:    // ✗ 오류! 변수 사용 불가
    case Y:    // ✓ const 상수는 가능
    case 15:   // ✓ 리터럴 상수 가능
}
```

### Q3: default는 반드시 마지막에 있어야 하나요?
**A:** 아니요, 어디에든 있을 수 있지만 관례상 마지막에 둡니다.
```cpp
switch (n) {
    default:     // 맨 앞에 있어도 동작함
        cout << "기타" << endl;
        break;
    case 1:
        cout << "하나" << endl;
        break;
}
```

### Q4: switch 안에 switch를 중첩할 수 있나요?
**A:** 네, 가능합니다. 하지만 가독성을 위해 함수로 분리하는 것을 권장합니다.

### Q5: break를 빠뜨리는 실수를 방지하려면?
**A:**
1. 모든 case에 break 작성을 습관화
2. IDE 경고 옵션 활성화
3. C++17의 `[[fallthrough]]` 속성 사용
```cpp
case 1:
    cout << "1" << endl;
    [[fallthrough]];  // 의도적인 fall-through임을 명시
case 2:
    cout << "2" << endl;
    break;
```

---

## 8. 핵심 정리

```
┌────────────────────────────────────────────────────────────────────────┐
│                       switch 문 핵심 정리                               │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ✅ switch 문법:                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ switch (변수) {                                                 │  │
│  │     case 값1: 명령문; break;                                    │  │
│  │     case 값2: 명령문; break;                                    │  │
│  │     default: 명령문; break;                                     │  │
│  │ }                                                               │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ✅ 핵심 키워드:                                                        │
│     • switch: 조건 분기 시작                                           │
│     • case: 각 값에 대한 분기점                                         │
│     • break: switch 종료 (없으면 fall-through)                         │
│     • default: 모든 case에 해당 안 될 때                               │
│                                                                        │
│  ✅ 사용 가능 타입: int, char, short, long, enum                       │
│  ✅ 사용 불가 타입: float, double, string, 배열                        │
│                                                                        │
│  ✅ switch 사용 시기:                                                   │
│     • 하나의 변수를 여러 특정 값과 비교                                  │
│     • 메뉴 선택, 상태 코드, 명령어 처리                                  │
│                                                                        │
│  ✅ if-else 사용 시기:                                                  │
│     • 범위 비교 (>=, <=)                                               │
│     • 복합 조건 (&&, ||)                                               │
│     • 실수, 문자열 비교                                                 │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 체크리스트

- [ ] switch 문의 기본 문법을 이해했다
- [ ] break 문의 역할을 이해했다
- [ ] fall-through가 무엇인지 알고, 의도적으로 활용할 수 있다
- [ ] switch와 if-else의 차이를 알고 상황에 맞게 선택할 수 있다
- [ ] 문자(char)를 이용한 switch를 작성할 수 있다
- [ ] 메뉴 시스템을 switch로 구현할 수 있다

---

## 다음 시간 예고

**Day 2-2교시: for 반복문**
- for 문의 구조와 실행 순서
- 다양한 증감 방식
- 중첩 for 문
- 패턴 출력
